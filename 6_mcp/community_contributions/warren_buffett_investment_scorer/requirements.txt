# Warren Buffett Investment Scorer
# ---------------------------------
# The CORE tool needs NOTHING beyond the Python 3.9+ standard library.
# Just run:  python -m investment_scorer
#
# Optional extras (uncomment to install):
#
#   yfinance>=0.2.40   # richer valuation: trailing/forward P/E, price-to-book.
#                      # Without it, the valuation pillar uses a robust
#                      # price-based margin-of-safety proxy instead.
#
#   mcp[cli]>=1.5.0    # only needed to run mcp_server.py as an agent tool.
#                      # Already provided by the course's root environment.
